# multi-layer-prompt-detection
A multi-layer framework for detecting and preventing prompt injection in LLMs. It processes multimodal inputs, applies preprocessing, heuristic and semantic analysis (BERT + vector DB), and an LLM-based risk layer to block, sanitize, or allow inputs with output monitoring for safety.
